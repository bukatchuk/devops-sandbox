# Заведомо старые версии — стенд для опыта с проверкой зависимостей (SCA).
# В настоящем проекте такого файла быть не должно; здесь он нужен,
# чтобы сканеру было что находить.
urllib3==1.26.4
jinja2==3.0.0
requests==2.25.1
